#pragma once
#include "Fighter.h"
#include "Skill.h"
#include "Game.h"

int main() {
	Game streetFighter;
	/*char contGame = '\0';
	while (contGame != 'q') {
		int mode = selectMode();
		if (mode == 0) {//PVE
			play(roster, mode, contGame);
		}
		if (mode == 1) {//PVP
			play(roster, mode, contGame);
		}
		if (mode == 2) {//CVC
			play(roster, mode, contGame);
		}
		if (mode == 3) {//Tag-Team
			int ryu = selectFighter(roster);
		}
		if (mode == 4) {
			addFighter(roster);
			system("cls");
		}
		if (mode == 5) {
			printWinLoss(roster); 
		}
		
	}
	delete ryu;
	delete ken;
	for (int i = S-1; i >= 0; i--) {
		delete roster[i];
	}*/
	return 0; 
}
